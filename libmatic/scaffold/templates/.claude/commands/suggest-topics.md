---
description: 週次のテーマ候補収集 workflow を実行 (libmatic suggest-topics)
argument-hint: ""
---

`/suggest-topics` を Claude Code から実行するための薄い wrapper です。
内部で `libmatic suggest-topics` を呼びます。

!libmatic suggest-topics --config config/libmatic.yml
